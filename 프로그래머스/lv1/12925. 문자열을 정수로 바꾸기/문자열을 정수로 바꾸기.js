function solution(s) {
    // let nums = s.split('').map(el => {
    //     if(el === "+" || el === "-") return el
    //     else return parseInt(el)
    // })
    // if(nums[0] === '-') return parseInt(nums.join(''))
    // else return parseInt(nums.join(''))
    return parseInt(s)
}